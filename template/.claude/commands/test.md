Run the full test suite and report results.

Steps:
1. Run `uv run pytest tests/ -v --tb=short`
2. If any tests fail, analyze the failures and suggest fixes
3. If all pass, report the summary (number of tests, duration)
